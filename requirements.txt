# ── Python version ───────────────────────────────────────────────────────────
# REQUIRES PYTHON 3.9-3.12 (3.12 recommended).
# mediapipe 0.10.14 only ships wheels for cp39-cp312. On Python 3.13/3.14 the
# install fails with:
#   ERROR: Could not find a version that satisfies the requirement mediapipe==0.10.14
# On Streamlit Community Cloud, pick 3.12 in the "Advanced settings" dialog when
# you deploy. That version CANNOT be changed after deployment -- you have to
# delete the app and redeploy it.

streamlit==1.54.0
streamlit-webrtc==0.64.5
mediapipe==0.10.14
# MediaPipe 0.10.x declares `protobuf` with no upper bound, so a fresh install
# can pull protobuf 5.x. Protobuf 5 removed MessageFactory.GetPrototype, which
# MediaPipe's pose landmarker still calls -- the result is
#   AttributeError: 'MessageFactory' object has no attribute 'GetPrototype'
# and pose detection silently produces no landmarks (video shows, but no
# skeleton and reps stay at 0). Pin to the 4.25.x line that MediaPipe works with.
protobuf>=4.25.3,<5.0.0
opencv-python-headless==4.10.0.84
pandas==2.2.3
groq>=0.12.0
gtts==2.5.3
# Offline speech fallback -- drives the OS speech engine (SAPI5 on Windows,
# NSSpeechSynthesizer on macOS, espeak on Linux) so voice coaching still works
# with no internet. Optional: the app runs fine without it, just silent when
# gTTS can't be reached.
pyttsx3==2.98
python-dotenv==1.2.2
